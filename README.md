# Magento-CMS-Static-Block-Helper
This extension is used to call the helper function from CMS and Static Block section in admin panel.

# Example :
{{helper module="yourmodule" method="yourfunction" params="arg1,arg2,..etc"}}
